import * as React from 'react';

const FeatureAppDefinition = {
  create: () => {
    return ({
      render: () => (
        <div>Hello FeatureHub!</div>
      ),
    });
  },
};

export default FeatureAppDefinition;
